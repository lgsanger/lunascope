/**
 * Copy to config.js and fill in your Supabase project values so Reflection Void
 * shows posts from all users. Get them: Supabase dashboard → Project Settings → API.
 * Never commit config.js (see .gitignore).
 */
window.LUNASCOPE_VOID = {
  supabaseUrl: "https://YOUR_PROJECT_REF.supabase.co",
  supabaseAnonKey: "YOUR_ANON_PUBLIC_KEY",
};
